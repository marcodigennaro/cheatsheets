python -m pip install poetry

poetry init # creates project.toml file interactively
poetry install # creates virtual environment 
poetry env info # shows where venv lives 
poetry config virtualenvs.in-project true # generate venv in local folder

poetry shell # activate venv inside folder

poetry add requests # install package ; goes automatically into proj.toml

exit
poetry env list
